import "./imageSlider.scss";
import { homeSlideImage } from "../../../../app/data";


const ImageSlider = (props)=>{
     return (
        <div className="imageSlider">
        </div>
     );
}

export default ImageSlider;
